node_modules
.env
.env.*
data/
*.db
*.log
.git
.DS_Store

# Native apps (iOS/Android) — not part of the web server image
native/

# Not needed at runtime. seed/ and public/ are read by the server and must stay.
tests/
docs/
.claude/
# Root-level docs only (DEPLOY.md has held credentials, see forensics B8)
*.md

# Scratch files
*.tmp
*.bak
*.orig
*.swp
coverage/

public/build/
